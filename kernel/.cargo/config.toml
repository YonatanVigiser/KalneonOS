[unstable]
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]

[build]
target = "i386-kalneon_os.json"
target-dir = "../build/kernel"

[profile.release]
# Optimize build:
opt-level = "z"
debug = false
strip = "symbols"
